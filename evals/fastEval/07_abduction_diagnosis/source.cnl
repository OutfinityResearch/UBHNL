# Test 07: Medical Diagnosis
# STYLE: Natural "has X" predicates + multiple rules

Patient is a Domain.
p1 is a Patient.

# Diagnostic rules - natural "has" pattern
For any Patient p:
    If $p has Flu then $p has Fever.
    If $p has Flu then $p coughs.
    If $p has Cold then $p coughs.

# Observations - natural predicates
p1 has Fever.
p1 coughs.
