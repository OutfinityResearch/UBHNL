Proof ProbabilisticWeather:
    Given:
        Prob(Cloudy(today)) = 0.8.
        For all Day d: If Cloudy(d) then Rain(d).
    Apply:
        Probabilistic Modus Ponens.
    Derive:
        If Cloudy(today) then Rain(today).
    Therefore:
        Prob(Rain(today)) >= 0.8.
